import mongoose from "mongoose";
import { act } from "react";

const postSchema = new mongoose.Schema({
    userId : {
    },
    body : {
    },
    create : {
    } ,
    createdAt : {
    },
    updatedAt : {
    },
    media : {
    },
    active : {
    }, 
    fileType : {
    }
});